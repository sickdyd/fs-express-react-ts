{
  "private": true,
  "name": "fs-express-react-ts",
  "scripts": {
    "api:dev": "yarn workspace api run dev",
    "api:start": "yarn workspace api run start",
    "api:build": "yarn workspace api run build",
    "api:debug": "yarn workspace api run debug",
    "api:test:unit": "yarn workspace api run test",
    "customer:dev": "yarn workspace customer run dev",
    "customer:build": "yarn workspace customer run build",
    "customer:test": "yarn workspace customer run test"
  },
  "workspaces": [
    "api",
    "customer"
  ]
}
